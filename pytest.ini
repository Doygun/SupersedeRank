[pytest]
testpaths = tests
addopts = -m "not full_data"
markers =
    unit: fast tests with synthetic inputs or committed report files
    integration: tests touching a small slice of real raw data
    full_data: full scans of data/raw (minutes); run with: pytest -m full_data
